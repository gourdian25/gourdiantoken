// Package gourdiantoken provides a JWT-based token management system for access and refresh tokens.
//
// Features:
// - Creation and verification of access and refresh tokens
// - Configurable token expiration and claims
// - Support for both symmetric key signing (HMAC) and asymmetric key signing (RSA, ECDSA)
// - Integration with UUID for token and session IDs
// - Customizable token claims (e.g., roles, permissions)
package gourdiantoken
